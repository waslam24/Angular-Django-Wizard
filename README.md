# Angular-Django-Wizard
Assistant complet pour automatiser le déploiement d’une app Angular sur un backend Django : modifie settings.py et urls.py, transforme index.html, copie les assets, gère les backups, et propose une interface fluide et portable en un seul exécutable.
